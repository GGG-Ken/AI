# Windows Codex Archive

Safe Windows Codex exports: redacted config, global instructions, session index, and skill documentation/config files.

Raw full sessions, SQLite logs/state, credentials, sandbox secrets, caches, and runtime/plugin binaries are excluded or kept under `private-not-pushed/`.
