# AI

Personal AI/Codex configuration and safe notes.

This repository intentionally tracks only safe, shareable Codex configuration
exports and indexes. Raw Codex databases, credentials, full chat logs, runtime
caches, and sandbox secrets are kept locally under `codex/private-not-pushed/`
and ignored by Git.
