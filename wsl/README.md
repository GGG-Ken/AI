# WSL Codex Archive

Safe WSL Codex exports for `Ubuntu-20.04`: redacted config, global instructions, prompts/rules, session file index, and skill documentation/config files.

Raw full sessions and SQLite databases are copied locally under `codex/private-not-pushed/` and ignored by Git.
